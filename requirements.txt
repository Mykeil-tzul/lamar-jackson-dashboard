streamlit==1.33.0
pandas==2.2.2
numpy==1.26.4
duckdb==0.9.2          # lighter wheel; installs fast on Cloud
pyarrow==14.0.2
altair==5.2.0
python-dotenv==1.0.1
# (omit nfl_data_py for now; we’ll add it back after the app is live)
